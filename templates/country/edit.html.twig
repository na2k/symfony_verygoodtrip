{% extends 'base.html.twig' %}

{% block title %}Edit Country{% endblock %}

{% block body %}
    <h1>Modifier un pays</h1>

    {{ include('country/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

    <a class="btn btn-dark" href="{{ path('country_index') }}">Retour à la liste</a>

    {{ include('country/_delete_form.html.twig') }}
{% endblock %}
